{
  "name": "@cometh/slim-keystore",
  "version": "1.0.0",
  "description": "cometh keystore contracts",
  "private": true,
  "scripts": {
    "clean": "forge clean",
    "build": "forge build",
    "generate": "wagmi generate "
  },
  "devDependencies": {
    "@wagmi/cli": "^2.1.4"
  }
}
